# AUTO-GENERATED by tools/gen_ia2_artifacts.py — edit ia2_config.json, not this file.
# Modbus TCP device for slave 6 (1 channel(s)) behind the RTU-to-TCP gateway.
# 32-bit data types (f32/u32/i32) span two consecutive registers from `address`.

name = "cabinet_vfd"
protocol = "modbus"
slave_id = 6
poll_interval_ms = 50

[transport]
kind = "tcp"
host = "127.0.0.1"
port = 5020

[[channels]]
name = "vfd_cmd"
kind = "holding_register"
address = 4096
data_type = "u16"
